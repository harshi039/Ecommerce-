
Uploaded image
Rephrase this prompt 

Your prompt in the screenshot says:

Move onboarding tasks to "other tasks" tab. Rename "Other tasks" tab to "Account onboarding". Move Addupdate-user tasks to separate tab called "User onboarding". Onboarding tasks are: create-account/create-subscription create-network create-adgroup ou-placement tag-account enable-region share-ami kong-sync complete-onboarding check-vhub-limit update-aduser and create-service-account task --> user onboarding task.

Here is a clean rephrased version you can use (this should avoid the blocking issue):

Rephrased Prompt:

Move all onboarding-related tasks to the "Other Tasks" tab.
After moving them, rename the "Other Tasks" tab to "Account Onboarding".

Create a new tab called "User Onboarding" and move the Add/Update User tasks into this tab.

The tasks that belong to Account Onboarding include:

create-account / create-subscription

create-network

create-adgroup

ou-placement

tag-account

enable-region

share-ami

kong-sync

complete-onboarding

check-vhub-limit

update-aduser

create-service-account

The Add/Update User and Create Service Account tasks should be categorized under User Onboarding.
